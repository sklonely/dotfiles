[project]
name = "installer"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["typer[all]"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
installer = "installer.cli:app"